import React from 'react';
import { Container } from './style';

function Input({placeholder, width, height, fontSize}) {
  return (
    <Container placeholder={placeholder} width={width} height={height} fontSize={fontSize}>
      
    </Container>
  )
}

export default Input
